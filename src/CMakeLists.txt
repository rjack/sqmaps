PROJECT( sqmaps )

# find libxml2
FIND_PACKAGE( LibXml2 REQUIRED )
INCLUDE_DIRECTORIES( ${LIBXML2_INCLUDE_DIR} )
ADD_DEFINITIONS( ${LIBXML2_DEFINITIONS} )

ADD_EXECUTABLE ( sqm main.c ship.c )

TARGET_LINK_LIBRARIES( sqm ${LIBXML2_LIBRARIES})
